rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through the Python API (Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
